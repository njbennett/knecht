#!/bin/sh
set -e

# Pre-commit hook: Enforce tests pass and 100% line coverage

echo "Running tests..."
cargo test --quiet

echo "Checking coverage (requires 100%)..."
cargo llvm-cov --all-features --fail-under-lines 100 --summary-only

echo "✓ All checks passed"