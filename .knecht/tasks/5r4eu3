5r4eu3,open,Add --blocks flag to 'knecht add' for creating subtasks,"When creating a subtask, the natural workflow is 'knecht add --blocks parent-task ""subtask title""' to create the task AND link it in one command. Current workflow requires two commands: add, then block. Agent forgot to run the second command and user had to correct. The 'knecht add' output suggests the inverse relationship ('To make another task blocked by this') which doesn't help when creating subtasks.
Pain: Agent created 3 subtasks with parent mentioned in description but forgot knecht block - user had to correct",1,
