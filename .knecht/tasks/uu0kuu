uu0kuu,open,production_tasks_file_is_never_modified_by_tests test is fragile for format changes,"During storage format migration, this test failed because it assumed .knecht/tasks is a file. Had to completely rewrite it to handle directory-based storage. The test should be more format-agnostic or there should be a note that format changes require updating this test.",,Test handles both file and directory formats without needing rewrite
