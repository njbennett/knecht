259,open,"cw script creates merge commits when local main is behind origin After rebasing branch onto origin/main, the script tries to merge into local main which may be behind origin. This causes ff-only to fail, and the fallback instructions tell user to do a regular merge (creating merge commit). Fix: update local main with 'git pull --ff-only' before attempting the branch merge.",,,
