nj4nej,open,Schema version header for graceful format evolution,"Add a schema version header to task storage (e.g. '# schema:v2'). Migration logic reads header and handles old formats. Adding new fields doesn't require rewriting existing lines - old records stay in old format until touched. Benefits: schema changes don't cause full-file rewrites, backwards compatibility built in. Part of task-ezzj7w (eliminate merge conflicts).",,
