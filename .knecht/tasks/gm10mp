gm10mp,open,/upstream skill leaves uncommitted tasks in knecht project,"From storytime: After using /upstream to file tasks in knecht, the agent continued working in storytime. User had to explicitly ask to commit the knecht changes. The /upstream workflow should handle this - either auto-commit or provide clear instruction to commit before returning to downstream work.",,"After /upstream files tasks, it either commits them automatically OR reminds the agent to commit in knecht project before continuing"
