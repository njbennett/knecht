224,open,Agent checks for tests after feature is already implemented,"Anti-pattern: When a feature already exists, agent looks for tests 'to make sure' rather than trusting TDD was followed. This wastes time and misunderstands TDD - tests come BEFORE implementation, not after. If feature works, either test exists or it was already done. Pain: task-186 session - agent tried to verify test existed for already-working deliver success message.",,
