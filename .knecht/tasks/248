248,open,Agents dismiss issues as 'not knecht bugs' when reflection prompts them to file tasks,"Meta-problem: reflection prompt explicitly warns 'Did you dismiss something as not a knecht bug that you should file?' but agents (including me) still do it. I dismissed the acceptance criteria ambiguity as 'not a knecht bug' when I should have filed pain on task-169. The prompt catches this but doesn't prevent it.
Pain: During task-250 reflection, dismissed large test file issue as 'VS Code/tooling constraint' instead of recognizing it as a knecht codebase issue. User had to intervene to point this out.",1,
