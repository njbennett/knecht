b46lie,done,Append-only pain log to reduce merge conflicts,"Separate pain instances from tasks file into an append-only log (.knecht/pain). Pain instances would be appended, never modified. The tasks file would no longer need pain_count field - it would be computed from the pain log. Benefits: pain updates to different tasks never conflict, simpler pain tracking. Part of task-ezzj7w (eliminate merge conflicts).",,
