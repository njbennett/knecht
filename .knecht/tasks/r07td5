r07td5,open,Create script to start work on next task,"Currently have to run: ../knecht/scripts/cw $(knecht next | grep task- | awk '{ print $4}') - this should be a single script like 'knecht work' or similar.
