227,open,Update rules to prevent test-only tasks decoupled from features,"Agents should never create tasks like 'write integration tests for X' separate from implementing X. Tests should fail first and drive implementation (TDD). The rules/CLAUDE.md should explicitly prohibit this pattern. Incident: task-190 asked for deliver command tests without the feature work, wasting a session.",,
